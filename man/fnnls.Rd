% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fnnls.R
\name{fnnls}
\alias{fnnls}
\title{Fast Non-Negative Least Squares}
\usage{
fnnls(
  XtX,
  Xty,
  tol = 1e-06,
  max_iter = 1000,
  sum_to_constant = FALSE,
  constant = 1,
  lower_bound = FALSE,
  lb = 0
)
}
\arguments{
\item{XtX}{A symmetric positive definite matrix of dimensions k x k.}

\item{Xty}{A numeric vector of length k.}

\item{tol}{The convergence tolerance, default is 1e-6.}

\item{max_iter}{The maximum number of iterations, default is 1000.}

\item{sum_to_constant}{If TRUE all entries sum to 'constant', Default is FALSE.}

\item{constant}{If sum_to_constant is TRUE, all entries sum to this number. The default value is 1.}

\item{lower_bound}{If TRUE all entries bounded below by 'lb', otherwise they are nonnegative. The default value is FALSE.}

\item{lb}{If lower_bound is TRUE all entries are bounded below by 'lb'. The default value is0.}
}
\value{
non-negative numeric vector of length k
}
\description{
Solves the NNLS problem min ||Xty - XtX * w||^2 subject to w >= 0
using the Fast Non-Negative Least Squares algorithm of Bro & de Jong (1997).
}
\examples{
k <- 10
D <- 100
H   <- matrix(rnorm(k * D), nrow = k, ncol = D)
x   <- rnorm(D)
XtX <- H \%*\% t(H) + diag(1e-8, k)
Xty <- as.vector(H \%*\% x)
w   <- fnnls(XtX, Xty)

}
\references{
Bro, Rasmus & Jong, Sijmen. (1997). A Fast Non-negativity-constrained Least Squares Algorithm.
Journal of Chemometrics. 11. 393-401. 10.1002/(SICI)1099-128X(199709/10)11:53.0.CO;2-L.
}
